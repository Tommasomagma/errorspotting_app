"Tabellen visar ett samband mellan $x$ och $y$. Rita in dessa punkter i koordinatsystemet och koppla ihop dem så att de bildar en linje. \n\nVad är proportionalitetskonstanten?". Problem image: A table with two columns and two rows. The headers for the columns are 'x' and 'y'. The column under 'x' contains four values in sequence, and the column under 'y' contains corresponding values in another sequence.